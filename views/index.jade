script(type='text/javascript')
	var message = {
		action:'attack',
		position:[56.241, -8.141],
		status:'missed'
	};
	
script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.6.1/jquery.min.js')
script(src='/socket.io/socket.io.js')
script(type='text/javascript')
	var socket = new io.Socket('localhost', {port: 10721});
	socket.connect();
	socket.on('message', function(msg){
		console.log(msg);
	});

<p><a id="customAlert" href="#" onclick="socket.send(message)">publish customAlert</a></p>